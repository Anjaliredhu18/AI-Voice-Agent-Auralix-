## Packages
@vapi-ai/web | Voice AI SDK for browser-based assistants

## Notes
Vapi integration requires a Public Key and Assistant ID.
Keys are stored in localStorage for persistence.
Backend endpoints used for call logging (POST /api/calls).
